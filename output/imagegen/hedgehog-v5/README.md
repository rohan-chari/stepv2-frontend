# Hedgehog publication

Published at user request to CDN on 2026-09-14, version `913c434d04e4`. Backend asset commit `6f97a3c`; static file copied without service restart. Public PNG bytes and TestFlight manifest verified. Existing admin creation pipeline mirrored catalog successfully.

CHARACTER, active/testOnly/remoteOnly true; six 96×96 frames, baseline -0.03125. Price follows live Mouse: 1,000 coins before existing server discounts. No new power or scoring behavior. Granted once to exact Rohan and Nathan accounts using a locked, idempotent transaction; no coin or equipment mutations. Private audit retained on production host.

First/repeated authenticated character catalogs verified for both recipients: visible on current TestFlight, hidden on production and TestFlight without remote-asset capabilities. Production manifest excludes Hedgehog. No carrying app binary or app upload required. Flutter analyze clean; no integration tests run against production. Economy and code reviews: SOUND/SHIP.

## Manual placement checklist

1. As each recipient, reopen TestFlight → Shop → Characters → Hedgehog wardrobe. Confirm one complete centered animal and no quill/foot clipping throughout the loop.
2. Equip manually → Home. Verify a single character with feet grounded.
3. Inspect existing race cards, race detail and available team race slots in both facing directions; check no overlap with names or neighboring racers.
4. Inspect leaderboard and public-profile character slots; check scale and clipping.
5. Where compatible accessory/referral previews display Hedgehog, check body fit. Unavailable states remain untested.

Demo race and tab tutorials use capybara/corgi fixtures, and the wardrobe tutorial uses the default character; these cannot validate Hedgehog placement. Check both supported platforms wherever a test-channel build is available. Physical-device playback remains manual.
