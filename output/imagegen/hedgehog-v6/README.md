# Uniform brown quill revision

Published at user request on 2026-09-14, CDN version `91d6d13688f2`, asset-only backend commit `7f623cb`. Static PNG bytes and public TestFlight manifest verified. Admin PATCH changed only assetVersion; all other catalog metadata/policy remained unchanged and peer mirroring succeeded. Existing ownership grants are retained. TestFlight only, remoteOnly; production manifest excludes Hedgehog.

Six 96×96 frames, baseline -0.03125. Older immutable asset retained for cached manifests. No application restart, build or logic change. Existing placement checklist is in `../hedgehog-v5/README.md`; device playback remains manual.
